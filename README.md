
### Daily Knowledge - PHP, Laravel, Mysql - PostgreSQL, Redis   

```php 

final class Life
{
    private function toDo()
    {
        while (isAlive()) {
            return getStatus() !== 'calm'
                ? 'keep calm'
                : 'code it';
        }    
    }
}
    
```


